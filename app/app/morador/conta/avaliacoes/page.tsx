import { RatingsHistory } from '@/components/features/account/RatingsHistory';

export const metadata = {
  title: 'Minhas Avaliações',
};

export default function AvaliacoesPage() {
  return <RatingsHistory />;
}
